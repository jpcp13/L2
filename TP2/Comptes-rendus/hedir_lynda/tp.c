#include<stdlib.h>
#include<stdio.h>
#include<math.h>

 double fonction( double x)
	{double f;
	f=sqrt(1- x*x)
         return f; 
}
int main()
	{
		int a,b,y1,y2;
		    a=-0.5;
		    b=0.5;
		    y1=f(a);
                    y2=f(b);


			printf("y1 et y2 sont des images de a,b ")
			
			  
			 
		   
#define aire double (int n)
	double f
		return f
int main()
	{
	 double f,I;
	 int a,b,n;
	 I=(b-a)/n /* I est un intervalle*/
	 aire=0.0;
	 x1=a/*creation de deux variables necessaires au calcul*/
	 x2=0.0;
		for i<n;
		{
		 x2=a+I;
		 aire= I*(f(x1+x2)/2))
		 I= x1+x2;
		 return aire;

		 printf("A=%lf est le point milieu")
		
#define erreur (x):
		return double abs(int(x-0.96))
int main()
		{
                 double p;
                 int x,n; 
                 printf("%lf=p(x,n)")
                 if n>0; 
		 {return x*p(x,n-1)}
                  else;
                  {return 1;
			}
			return EXIT_SUCCES
                                      }
#define trapeze
int main()
	double f,T;
        int a,b,n;
        I=(b-a)/n;
        cout<<"T est l'aire de la fonction de trapeze">>
        for a<b;
               {T=(b-a)*(f(a)+f(b))/2;
		}
                 return EXIT_SUCCES
		 
